{
  "name": "@flight-data-collector/collector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/worker.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@flight-data-collector/core": "workspace:*",
    "@flight-data-collector/db": "workspace:*",
    "@flight-data-collector/providers": "workspace:*",
    "bullmq": "^5.8.0",
    "ioredis": "^5.4.1",
    "pino": "^9.2.0"
  }
}
