{
  "name": "@flight-data-collector/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@flight-data-collector/db": "workspace:*",
    "bullmq": "^5.8.0",
    "fastify": "^4.28.0",
    "ioredis": "^5.4.1",
    "pino": "^9.2.0",
    "zod": "^3.23.8"
  }
}
